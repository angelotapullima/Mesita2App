import 'package:flutter/material.dart';

class ReporteUtilidades extends StatelessWidget {
  const ReporteUtilidades({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Text('Utilidades'),
      ],
    );
  }
}
